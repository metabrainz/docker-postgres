#!/bin/sh

set -e

{{- range service "postgres-slave"}}
rsync \
    --archive \
    --ignore-existing \
    --password-file=/etc/rsync_password_file \
    "$1" \
    rsync://postgres@{{.Address}}:/pg_archive/$2
{{- end}}

exit 0
